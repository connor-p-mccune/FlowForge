// Optional dev seed — run manually: node src/db/seed.js
// Does nothing in Phase 1; add sample data here as needed.

require('../config/database') // ensures the database file + schema exist

console.log('Seed complete (nothing to seed in Phase 1)')

module.exports = {}
